validators:
  - name: dockerhub-pat
    rule_ids:
      - np.dockerhub.1
    http:
      method: GET
      url: "https://hub.docker.com/v2/repositories/namespaces"
      auth:
        type: bearer
        secret_group: "token"
      success_codes: [200]
      failure_codes: [401, 403]
