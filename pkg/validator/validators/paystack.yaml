validators:
  - name: paystack-api-key
    rule_ids:
      - kingfisher.paystack.1
    http:
      method: GET
      url: https://api.paystack.co/balance
      auth:
        type: bearer
        secret_group: "token"
      success_codes: [200]
      failure_codes: [401, 403]
