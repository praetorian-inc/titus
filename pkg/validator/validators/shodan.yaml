validators:

- name: shodan-api-key
  rule_ids:
    - kingfisher.shodan.1

  http:
    method: GET
    url: "https://api.shodan.io/api-info"
    auth:
      type: query
      secret_group: "1"
      query_param: "key"

    success_codes: [200]
    failure_codes: [401, 403]
