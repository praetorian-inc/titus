validators:

- name: cohere-api-key
  rule_ids:
    - kingfisher.cohere.1

  http:
    method: GET
    url: "https://api.cohere.com/v1/connectors"
    auth:
      type: bearer
      secret_group: "token"

    success_codes: [200]
    failure_codes: [401, 403]
