validator:
  name: credentials
  description: Validates credentials in URLs by checking if the password field has sufficient entropy
  rules:
    - rule_id: kingfisher.credentials.1
      secret_group: password
      validation:
        min_length: 3
        description: Validates that the password component of a URL credential has minimum length and entropy
